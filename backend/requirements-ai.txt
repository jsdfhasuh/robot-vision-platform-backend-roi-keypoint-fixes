# Compatibility file for older install docs/scripts.
# ONNX Runtime is now part of requirements.txt because YOLO detectors and
# /api/system/self-check both expect it in the backend runtime.

-r requirements.txt
